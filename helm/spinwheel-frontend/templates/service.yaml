apiVersion: v1
kind: Service
metadata:
  name: {{ include "spinwheel-frontend.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "spinwheel-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "spinwheel-frontend.selectorLabels" . | nindent 4 }}
