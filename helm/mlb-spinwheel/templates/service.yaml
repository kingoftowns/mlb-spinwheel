apiVersion: v1
kind: Service
metadata:
  name: {{ include "mlb-spinwheel.fullname" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "mlb-spinwheel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mlb-spinwheel.selectorLabels" . | nindent 4 }}
